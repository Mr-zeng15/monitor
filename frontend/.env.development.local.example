# ★ 2026-09-16：前端 API 基址默认已是【相对路径 /api】，正常情况下你不需要这个文件。
#
#   1. 生产/部署：前端产物里 API 基址 = 相对路径 /api
#      → 浏览器请求 <当前页面同源>/api/...，由 Django 自己提供
#      → 后端换端口、换机器、换 IP 都不需要改前端代码、不需要重新构建。
#
#   2. 开发（npm run dev，5173）：vite.config.js 里 '/api' 代理转发到后端，
#      代理目标默认 http://localhost:8000，可用环境变量覆盖：
#        VITE_DEV_BACKEND=http://127.0.0.1:8001 npm run dev
#
#   3. 只有需要把前端指向【另一个地址的后端】（例如 ABL Mock 服务）时，
#      才复制本文件为 .env.development.local 并取消下面一行的注释。用完请删除。
#
# VITE_API_BASE=http://127.0.0.1:8001/api

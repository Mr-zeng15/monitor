/* ══════════════════════════════════════════════════════════════════════
   ★★ 机器人图片（base64 版）—— 你只需要改这一行 ↓↓↓

   怎么填（3 步）：
     1) 把你拿到的整串 base64 粘到下面 export default 后面那对单引号【中间】。
     2) 必须【一行到底】：删掉换行/空格/Tab（编辑器里长串会自动折行显示，那是显示效果，不是真的换行，只要你不敲回车就行）。
     3) 保存 → 重新 build + 同步产物 → 浏览器 F5（见 机器人图片替换指南.md）。

   规则（照抄即可）：
     · 保留开头的  data:image/png;base64,  这一段，不要删、不要改大小写。
     · 整串要放在【单引号】里；如果你的 base64 里出现了单引号 '（正常不会），
       就改用双引号，或者把那个单引号写成 \' 。
     · 只放一张，16 张卡片共用它（页面上自动出现 16 次）。
     · 留空（就是现在这样 '' ）= 自动回退用 /assets/robot.png，再没有就隐藏图片位，不会留破图。
     · 本文件是 js 模块、不是页面，改完【不需要】动 Vue 文件，更不需要 npm 装包：
       这里属于构建期静态内联（Vite 打包），不是运行期读文件，所以离线环境也能用。

   示例（下面这行是【假数据】，仅供看清格式，请整段替换成你自己的真串）：
     export default 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA...（后面很长）...ErkJggg=='
   ══════════════════════════════════════════════════════════════════════ */

export default ''
